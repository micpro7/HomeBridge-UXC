name: Build Lean Homebridge UXC Bundle

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    name: Build OCI Bundle
    runs-on: ubuntu-latest

    permissions:
      contents: write

    env:
      QEMU_CPU: cortex-a72

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Podman and QEMU Static
        run: |
          sudo apt-get update
          sudo apt-get install -y podman qemu-user-static

      - name: Build Lean Rootfs inside Alpine ARM64
        run: |
          set -e

          mkdir -p homebridge-bundle/rootfs

          # Dynamically track the absolute latest official Alpine release
          CONTAINER_ID=$(podman run --platform linux/arm64 -d docker.io/alpine:latest sleep infinity)

          podman exec $CONTAINER_ID apk update
          
          # Install nodejs-current to grab the latest upstream features instead of old LTS lines
          podman exec $CONTAINER_ID apk add --no-cache \
            nodejs-current \
            npm \
            python3 \
            make \
            g++ \
            dbus

          podman exec $CONTAINER_ID npm install -g --unsafe-perm \
            homebridge \
            homebridge-config-ui-x

          # Foolproof version extraction looking through standard Alpine global npm install paths
          HB_VERSION=$(podman exec $CONTAINER_ID node -p "
            try { require('/usr/local/lib/node_modules/homebridge-config-ui-x/package.json').version }
            catch(e) { require('/usr/lib/node_modules/homebridge-config-ui-x/package.json').version }
          " | tr -d '\r' | xargs)
          
          HB_BIN="/usr/bin/homebridge"

          # Fallback check if string parsing fails completely
          if [ -z "$HB_VERSION" ] || [ "$HB_VERSION" = "undefined" ]; then
            HB_VERSION="latest"
          fi

          echo "VERSION=$HB_VERSION" >> $GITHUB_ENV
          echo "HB_BIN=$HB_BIN" >> $GITHUB_ENV

          echo "Homebridge Bundle Version: $HB_VERSION"
          echo "Homebridge Binary Path    : $HB_BIN"

          podman exec $CONTAINER_ID mkdir -p /var/lib/homebridge

          podman exec -i $CONTAINER_ID sh <<'EOF'
          cat > /var/lib/homebridge/config.json <<'JSON'
          {
            "bridge": {
              "name": "Homebridge",
              "username": "0E:96:AD:6D:D8:9C",
              "port": 51826,
              "pin": "031-45-154"
            },
            "platforms": [
              {
                "platform": "config",
                "name": "Config",
                "port": 8581,
                "sudo": false
              }
            ],
            "accessories": []
          }
          JSON
          EOF

          # Drop compiler toolkit components to protect precious router storage blocks
          podman exec $CONTAINER_ID apk del make g++
          podman exec $CONTAINER_ID npm cache clean --force || true
          podman exec $CONTAINER_ID rm -rf /root/.npm /var/cache/apk/* /tmp/*

          podman export "$CONTAINER_ID" | tar -C homebridge-bundle/rootfs -xf -
          podman rm -f "$CONTAINER_ID"

      - name: Generate OCI config.json
        run: |
          cat << 'EOF' > homebridge-bundle/config.json
          {
            "ociVersion": "1.0.2",
            "process": {
              "terminal": false,
              "user": {
                "uid": 0,
                "gid": 0
              },
              "args": [
                "${{ env.HB_BIN }}",
                "-U",
                "/var/lib/homebridge"
              ],
              "env": [
                "PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin",
                "HOME=/root"
              ],
              "cwd": "/",
              "capabilities": {
                "bounding": [
                  "CAP_CHOWN",
                  "CAP_SETUID",
                  "CAP_SETGID",
                  "CAP_NET_BIND_SERVICE"
                ],
                "effective": [
                  "CAP_CHOWN",
                  "CAP_SETUID",
                  "CAP_SETGID",
                  "CAP_NET_BIND_SERVICE"
                ],
                "inheritable": [
                  "CAP_CHOWN",
                  "CAP_SETUID",
                  "CAP_SETGID",
                  "CAP_NET_BIND_SERVICE"
                ],
                "permitted": [
                  "CAP_CHOWN",
                  "CAP_SETUID",
                  "CAP_SETGID",
                  "CAP_NET_BIND_SERVICE"
                ]
              }
            },
            "root": {
              "path": "rootfs",
              "readonly": false
            },
            "mounts": [
              {
                "destination": "/proc",
                "type": "proc",
                "source": "proc"
              },
              {
                "destination": "/dev",
                "type": "tmpfs",
                "source": "tmpfs",
                "options": [
                  "nosuid",
                  "strictatime",
                  "mode=755",
                  "size=65536k"
                ]
              },
              {
                "destination": "/sys",
                "type": "sysfs",
                "source": "sysfs",
                "options": [
                  "nosuid",
                  "nodev",
                  "noexec",
                  "ro"
                ]
              },
              {
                "destination": "/var/lib/homebridge",
                "type": "bind",
                "source": "/opt/homebridge",
                "options": [
                  "rbind",
                  "rw"
                ]
              }
            ],
            "linux": {
              "namespaces": [
                { "type": "pid" },
                { "type": "ipc" },
                { "type": "uts" },
                { "type": "mount" }
              ]
            }
          }
          EOF

      - name: Package Bundle
        run: |
          tar -cf "homebridge-uxc-bundle-v${{ env.VERSION }}-arm64.tar" \
            -C homebridge-bundle .

      - name: Update Latest Tag
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"

          # Force push the specific tag format matching your other build workflow
          git tag -f "homebridge-arm64-${{ env.VERSION }}"
          git push origin "homebridge-arm64-${{ env.VERSION }}" --force

      - name: Publish Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG_NAME="homebridge-arm64-${{ env.VERSION }}"
          
          if ! gh release view "$TAG_NAME" >/dev/null 2>&1; then
            gh release create "$TAG_NAME" \
              --title "Homebridge ARM64 v${{ env.VERSION }}" \
              --notes "Automated rolling build of the latest UXC native bundle framework matching core image tracking lines."
          else
            gh release edit "$TAG_NAME" \
              --title "Homebridge ARM64 v${{ env.VERSION }}"
          fi

          gh release upload "$TAG_NAME" \
            "homebridge-uxc-bundle-v${{ env.VERSION }}-arm64.tar" \
            --clobber
